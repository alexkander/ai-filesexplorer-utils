services:
  web:
    build:
      context: .
      target: runner
      network: host
    ports:
      - '3000:3000'
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ${MYFILES}:${MYFILES}
