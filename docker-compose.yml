services:
  web:
    build:
      context: .
      target: dev
      network: host
    network_mode: host
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
      - ${MYFILES}:${MYFILES}
    environment:
      - WATCHPACK_POLLING=true
